server:
  port: 9120

spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:postgresql://localhost:9110/estate_management
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
